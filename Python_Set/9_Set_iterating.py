# We can iterate over a set to access its elements.
# Sets are unordered that is why the elements may be returned in any order.

A = {1,3,5,7,9,11,12}

for item in A:
    print(f"Item of the set: {item}")