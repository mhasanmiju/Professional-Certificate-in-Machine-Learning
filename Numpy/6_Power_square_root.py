import numpy as np

array = np.array([[5, 7, 9], [2, 4, 6]])

power_array = np.power(array, 3)
square_root = np.sqrt(array)

print(f"Original Array: {array}")
print(f"Power Array: {power_array}")
print(f"Square root Array: {square_root}")